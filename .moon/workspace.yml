$schema: "./cache/schemas/workspace.json"

projects: ["packages/*"]

vcs:
  defaultBranch: "master"
  provider: "github"
  hooks:
    pre-commit:
      - "pnpm exec oxfmt"
    commit-msg:
      - "pnpm exec commitlint --edit $ARG1"
