id: "root"

language: javascript
layer: tool

tasks:
  lint:
    command: "oxlint ."
    preset: utility

  lint-fix:
    command: "oxlint --fix ."
    preset: utility

  format:
    command: "oxfmt ."
    preset: utility

  format-check:
    command: "oxfmt --check ."
    preset: utility
