name: diagoriente
version: 0.0.0

# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, grok-build, hermes, kiro, opencode, vscode, windsurf
targets:
  - codex
  - claude
  - vscode

dependencies:
  apm:
    - microsoft/aspire-skills#v0.0.2
  mcp:
    - name: "moon"
      registry: false
      transport: stdio
      command: "moon"
      args: ["mcp"]
      env:
        MOON_WORKSPACE_ROOT: .
    - name: "aspire"
      registry: false
      transport: stdio
      command: "aspire"
      args: ["agent", "mcp"]

includes: auto
scripts: {}
